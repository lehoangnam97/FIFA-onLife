import React,{Component} from 'react'
import {MainTabNavigator} from "../config/mainTabNavigator";

export default class Main extends Component{
    render(){
        return(
            <MainTabNavigator/>
        );
    }
}